<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="140dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginHorizontal="20dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    android:clickable="true"
    android:backgroundTint="@android:color/darker_gray"
    android:foreground="?selectableItemBackground"
    android:id="@+id/featured_article_container">

    <ImageView
        android:id="@+id/featured_article_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/test_img"
        android:scaleType="centerCrop" />

    <com.google.android.material.card.MaterialCardView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:cardBackgroundColor="#80D50000"
        android:layout_margin="8dp">

        <TextView
            android:id="@+id/featured_article_tag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Terpopuler"
            android:textSize="12sp"
            android:textColor="?colorOnPrimary"
            android:paddingHorizontal="8dp"
            android:paddingVertical="4dp"
            android:layout_gravity="center"/>

    </com.google.android.material.card.MaterialCardView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:background="#80AAAAAA"
        android:paddingVertical="8dp">

        <TextView
            android:id="@+id/featured_article_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Ini dia alasan yang membuat suami bertah dirumah"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="?colorOnSecondary"
            android:textSize="16sp"
            android:paddingHorizontal="12dp" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>